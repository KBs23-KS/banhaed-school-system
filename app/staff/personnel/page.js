"use client";import {useEffect,useState} from "react";import StaffShell from "@/components/StaffShell";import {staffFetch} from "@/lib/apiClient";export default function Page(){const[rows,setRows]=useState([]),[q,setQ]=useState("");async function load(){const d=await staffFetch(`/api/personnel?q=${encodeURIComponent(q)}`);setRows(d.personnel)}useEffect(()=>{load()},[]);return <StaffShell><div className="page-head"><small>PERSONNEL DIRECTORY</small><h1>ข้อมูลบุคลากร</h1><p>ข้อมูลประวัติ ตำแหน่ง กลุ่มบริหาร และเอกสารสำคัญ</p></div><div className="filters"><input placeholder="ค้นหาชื่อ ตำแหน่ง กลุ่มบริหาร" value={q} onChange={e=>setQ(e.target.value)}/><button onClick={load}>ค้นหา</button></div><section className="people-grid">{rows.map(p=><a href={`/staff/personnel/${p.id}`} className="person-card" key={p.id}><div className="person-avatar">{p.firstName?.[0]||"?"}</div><div><b>{p.prefix}{p.firstName} {p.lastName}</b><span>{p.position||"-"} {p.academicRank?`• ${p.academicRank}`:""}</span><small>{p.managementGroup||"ยังไม่ระบุกลุ่มบริหาร"}</small></div></a>)}</section></StaffShell>}
