"use client";
import { useState } from "react";
import UiIcon from "@/components/UiIcon";
export default function Bootstrap(){
 const [f,setF]=useState({secret:"",username:"admin",password:"",displayName:"ผู้ดูแลระบบ"}),[m,setM]=useState(""),[busy,setBusy]=useState(false);
 async function go(e){e.preventDefault();setBusy(true);setM("กำลังสร้าง Admin...");try{const r=await fetch("/api/bootstrap",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)}),d=await r.json();setM(r.ok?"สร้าง Admin สำเร็จ กลับไปหน้าเข้าสู่ระบบได้เลย":d.error||"สร้างไม่สำเร็จ")}catch{setM("เชื่อมต่อระบบไม่สำเร็จ")}finally{setBusy(false)}}
 return <main className="bootstrap-page"><div className="bootstrap-bg"/><form onSubmit={go} className="bootstrap-card"><img src="/school-logo.png" alt="ตราโรงเรียน"/><small>FIRST ADMIN SETUP</small><h1>ตั้งค่าผู้ดูแลระบบคนแรก</h1><p>ใช้หน้านี้ครั้งเดียวหลัง Deploy และตั้ง Environment Variables เรียบร้อย</p><label>Bootstrap Secret<input type="password" value={f.secret} onChange={e=>setF({...f,secret:e.target.value})}/></label><label>Username<input value={f.username} onChange={e=>setF({...f,username:e.target.value})}/></label><label>ชื่อที่แสดง<input value={f.displayName} onChange={e=>setF({...f,displayName:e.target.value})}/></label><label>Password<input type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label><button className="soft-primary full" disabled={busy}>{busy?"กำลังสร้าง...":"สร้าง Admin คนแรก"}<UiIcon name="shield" size={18}/></button>{m&&<div className="bootstrap-message">{m}</div>}<a href="/"><UiIcon name="back" size={15}/>กลับหน้าหลัก</a></form></main>
}
